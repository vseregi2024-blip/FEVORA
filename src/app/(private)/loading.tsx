export default function PrivateLoading() {
  return <main className="empty-state" aria-live="polite"><span className="empty-mark">…</span><h2>Загружаем FEVORA</h2><p>Проверяем актуальные данные.</p></main>;
}
