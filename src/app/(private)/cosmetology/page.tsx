import Link from "next/link";
import { AppIcon } from "@/components/ui/icons";
import { SectionHeader } from "@/components/ui/section-header";
import { formatMoney } from "@/lib/money";
import { requireUser } from "@/server/auth";
import { getCosmetologyAppointments, getCosmetologyDashboard } from "@/server/cosmetology";
const labels={TODAY:"Сегодня",WEEK:"Неделя",MONTH:"Месяц",YEAR:"Год"} as const;
export default async function CosmetologyPage({searchParams}:{searchParams:Promise<{period?:string}>}){const u=await requireUser(),p=await searchParams,period=p.period&&p.period in labels?p.period as keyof typeof labels:"MONTH",[d,appointments]=await Promise.all([getCosmetologyDashboard(u.id,period),getCosmetologyAppointments(u.id)]),scheduled=appointments.filter(x=>x.status==="SCHEDULED");return <><header className="page-header"><div><p className="eyebrow">Косметология</p><h1>Записи, товары и прибыль</h1><p className="muted">Сначала запись, затем фактическая процедура: только при закрытии появятся доход и списание материалов.</p></div><Link className="button primary" href="/cosmetology/visits">＋ Запись</Link></header><section className="metric-grid family-summary"><article><span>Записей ожидают</span><strong>{scheduled.length}</strong></article><article><span>Доход</span><strong className="income">{formatMoney(d.report.income)}</strong></article><article><span>Себестоимость</span><strong>{formatMoney(d.report.cost)}</strong></article><article><span>Прибыль</span><strong>{formatMoney(d.report.result)}</strong></article></section><p className="summary-note">Денежные расходы за период: {formatMoney(d.report.cashExpense)}. Использование материалов не создаёт второй расход денег.</p><SectionHeader eyebrow="Записи" title="Ближайшие клиенты" action={<Link href="/cosmetology/visits" className="text-link">Все записи</Link>}/><div className="transaction-list">{scheduled.slice(0,5).map(v=><Link href={`/cosmetology/visits/${v.id}`} className="transaction-row" key={v.id}><span><b>{v.procedureName}</b><small>{v.client.displayName??`${v.client.lastName} ${v.client.firstName}`} · {v.operationDate.toLocaleDateString("ru-RU")}{v.scheduledTime?` · ${v.scheduledTime}`:""}</small></span><strong>Закрыть →</strong></Link>)}{!scheduled.length&&<p className="empty">Нет активных записей. Добавьте клиента в календарь.</p>}</div><SectionHeader eyebrow="Разделы" title="Косметология"/><section className="poultry-section-grid"><Link href="/cosmetology/visits" className="poultry-section-card"><AppIcon name="sales"/><b>Записать клиента</b><span>Без списаний и дохода</span></Link><Link href="/cosmetology/inventory" className="poultry-section-card"><AppIcon name="feed"/><b>Товары</b><span>{d.items.length} поз. на складе</span></Link><Link href="/cosmetology/clients" className="poultry-section-card"><AppIcon name="projects"/><b>Клиенты</b><span>{d.clients.length} в базе</span></Link><Link href="/cosmetology/procedures" className="poultry-section-card"><AppIcon name="incubation"/><b>Услуги и техкарты</b><span>Шаблоны и расходники</span></Link><Link href="/cosmetology/expenses" className="poultry-section-card"><AppIcon name="money"/><b>Операционные расходы</b><span>Аренда, реклама, поездки</span></Link><Link href="/cosmetology/analytics" className="poultry-section-card"><AppIcon name="reports"/><b>Статистика</b><span>Доход, прибыль, склад</span></Link></section></>}
