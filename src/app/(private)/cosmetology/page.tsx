import Link from "next/link";
import { AppIcon } from "@/components/ui/icons";
import { SectionHeader } from "@/components/ui/section-header";
import { formatMoney } from "@/lib/money";
import { requireUser } from "@/server/auth";
import { getCosmetologyDashboard } from "@/server/cosmetology";
const labels={TODAY:"Сегодня",WEEK:"Неделя",MONTH:"Месяц",YEAR:"Год"} as const;
export default async function CosmetologyPage({searchParams}:{searchParams:Promise<{period?:string}>}){const u=await requireUser(),p=await searchParams,period=p.period&&p.period in labels?p.period as keyof typeof labels:"MONTH",d=await getCosmetologyDashboard(u.id,period);return <><header className="page-header"><div><p className="eyebrow">Косметология</p><h1>Финансы и процедуры</h1><p className="muted">Один визит — один доход, материалы списываются по факту.</p></div><Link className="button secondary" href="/cosmetology/analytics">Аналитика</Link></header><form className="period-tabs" action="/cosmetology">{Object.entries(labels).map(([v,l])=><button key={v} name="period" value={v} className={period===v?"active":""}>{l}</button>)}</form><section className="metric-grid family-summary"><article><span>Доход</span><strong className="income">{formatMoney(d.report.income)}</strong></article><article><span>Себестоимость</span><strong>{formatMoney(d.report.cost)}</strong></article><article><span>Опер. расходы</span><strong className="expense">{formatMoney(d.report.operationalExpense)}</strong></article><article><span>Прибыль</span><strong>{formatMoney(d.report.result)}</strong></article></section><p className="summary-note">Денежные расходы за период: {formatMoney(d.report.cashExpense)}. Использование материалов не создаёт второй расход денег.</p><SectionHeader eyebrow="Разделы" title="Косметология"/><section className="poultry-section-grid"><Link href="/cosmetology/visits" className="poultry-section-card"><AppIcon name="sales"/><b>Провести процедуру</b><span>Клиент, оплата и материалы</span></Link><Link href="/cosmetology/inventory" className="poultry-section-card"><AppIcon name="feed"/><b>Препараты и расходники</b><span>{d.items.length} поз. на складе</span></Link><Link href="/cosmetology/clients" className="poultry-section-card"><AppIcon name="projects"/><b>Клиенты</b><span>{d.clients.length} в базе</span></Link><Link href="/cosmetology/procedures" className="poultry-section-card"><AppIcon name="incubation"/><b>Прайс и процедуры</b><span>Шаблоны и расходники</span></Link><Link href="/cosmetology/expenses" className="poultry-section-card"><AppIcon name="money"/><b>Операционные расходы</b><span>Аренда, реклама, поездки</span></Link><Link href="/cosmetology/analytics" className="poultry-section-card"><AppIcon name="reports"/><b>Отчёты</b><span>Процедуры, клиенты, склад</span></Link></section><SectionHeader eyebrow="История" title="Последние визиты"/><div className="transaction-list">{d.visits.slice(0,5).map(v=><Link href={`/cosmetology/visits/${v.id}`} className="transaction-row" key={v.id}><span><b>{v.procedureName}</b><small>{v.client.lastName} {v.client.firstName} · {v.operationDate.toLocaleDateString("ru-RU")}</small></span><strong className="income">+{formatMoney(v.paymentAmount.toString())}</strong></Link>)}</div>{!d.visits.length&&<p className="empty">Визитов пока нет. Проведите первую процедуру.</p>}</>}
