import Link from "next/link";
import { EmptyState } from "@/components/ui/empty-state";
import { formatMoney } from "@/lib/money";
import { todayInputValue } from "@/lib/dates";
import { requireUser } from "@/server/auth";
import { getCosmetologyDashboard } from "@/server/cosmetology";
import { createCosmetologyVisitAction } from "../actions";
export default async function VisitsPage(){const u=await requireUser(),d=await getCosmetologyDashboard(u.id),clients=d.clients,templates=d.templates;return <><header className="page-header"><div><p className="eyebrow">Косметология · Визиты</p><h1>Провести процедуру</h1><p className="muted">Доход, визит и списание материалов создаются одной операцией.</p></div><Link href="/cosmetology" className="button secondary">Назад</Link></header><section className="family-grid"><article className="app-card"><h2>Новый визит</h2><form action={createCosmetologyVisitAction} className="compact-form"><label>Клиент<select name="clientId" defaultValue=""><option value="">Создать нового клиента ниже</option>{clients.map(c=><option key={c.id} value={c.id}>{c.lastName} {c.firstName} · {c.phone}</option>)}</select></label><div className="form-grid"><label>Фамилия нового клиента<input name="newClientLastName" placeholder="Если нет в списке"/></label><label>Имя нового клиента<input name="newClientFirstName"/></label></div><label>Телефон нового клиента<input name="newClientPhone" type="tel"/></label><label>Шаблон процедуры<select name="procedureTemplateId" defaultValue=""><option value="">Без шаблона</option>{templates.map(t=><option key={t.id} value={t.id}>{t.name}{t.basePrice?` · ${t.basePrice} грн`:""}</option>)}</select></label><div className="form-grid"><label>Название процедуры<input name="procedureName" required placeholder="Botox Full Face"/></label><label>Оплата<input name="paymentAmount" required inputMode="decimal" placeholder="5200"/></label></div><div className="form-grid"><label>Основной препарат<select name="itemId" defaultValue=""><option value="">Не использовать</option>{d.items.filter(i=>i.currentQuantity.gt(0)).map(i=><option key={i.id} value={i.id}>{i.name} · остаток {i.currentQuantity.toString()}</option>)}</select></label><label>Фактическое количество<input name="usageQuantity" inputMode="decimal" placeholder="35"/></label></div><label>Дата<input name="operationDate" type="date" required defaultValue={todayInputValue()}/></label><label>Комментарий<input name="comment"/></label><p className="summary-note">Стандартные расходники из шаблона будут списаны автоматически. Основной препарат укажите фактически.</p><button className="button primary">Сохранить процедуру</button></form></article><article className="app-card"><h2>Не угадываем</h2><p className="muted">Если клиент, процедура или препарат неоднозначны, выберите их вручную. Остаток не может стать отрицательным.</p></article></section><section className="section-header"><div><p className="eyebrow">История</p><h2>Визиты за период</h2></div></section><div className="transaction-list">{d.visits.map(v=><Link href={`/cosmetology/visits/${v.id}`} className="transaction-row" key={v.id}><span><b>{v.procedureName}</b><small>{v.client.lastName} {v.client.firstName} · {v.operationDate.toLocaleDateString("ru-RU")} · себестоимость {formatMoney(v.costAmount.toString())}</small></span><strong className="income">+{formatMoney(v.paymentAmount.toString())}</strong></Link>)}</div>{!d.visits.length&&<EmptyState title="Визитов пока нет" description="Проведите первую процедуру — склад и доход обновятся вместе."/>}</>}
