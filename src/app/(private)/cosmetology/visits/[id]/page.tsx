import Link from "next/link";
import { ConfirmationDialog } from "@/components/ui/confirmation-dialog";
import { formatMoney } from "@/lib/money";
import { requireUser } from "@/server/auth";
import { getCosmetologyVisit } from "@/server/cosmetology";
import { deleteCosmetologyVisitAction } from "../../actions";
export default async function VisitPage({params}:{params:Promise<{id:string}>}){const u=await requireUser(),{id}=await params,v=await getCosmetologyVisit(u.id,id);return <><header className="page-header"><div><p className="eyebrow">Косметология · Визит</p><h1>{v.procedureName}</h1><p className="muted">{v.client.lastName} {v.client.firstName} · {v.operationDate.toLocaleDateString("ru-RU")}</p></div><Link href="/cosmetology/visits" className="text-link">К визитам</Link></header><section className="metric-grid"><article><span>Доход</span><strong className="income">{formatMoney(v.paymentAmount.toString())}</strong></article><article><span>Себестоимость</span><strong>{formatMoney(v.costAmount.toString())}</strong></article><article><span>Прибыль</span><strong>{formatMoney(v.profitAmount.toString())}</strong></article></section><section className="app-card"><h2>Списано</h2><div className="mini-list">{v.usages.map(x=><div key={x.id}><span>{x.item.name} · {x.quantity.toString()}</span><b>{formatMoney(x.totalCost.toString())}</b></div>)}{!v.usages.length&&<p className="muted">Материалы не списывались.</p>}</div></section><div className="danger-zone"><h2>Удаление</h2><p>Доход будет исключён из отчётов, а материалы вернутся на склад.</p><ConfirmationDialog action={deleteCosmetologyVisitAction} fieldName="id" fieldValue={v.id} trigger="Удалить визит" title="Удалить визит?" description="Доход будет исключён из расчётов, а материалы вернутся на склад." confirmLabel="Удалить" triggerClassName="text-link"/></div></>}
